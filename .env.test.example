# ============================================================================
# TEST ENVIRONMENT VARIABLES
# ============================================================================
# This file provides mock credentials for local E2E testing and CI environments.
#
# USAGE:
#   cp .env.test.example .env.test
#   npm run test:e2e
#
# IMPORTANT:
#   - These are MOCK values for testing only
#   - DO NOT use real Firebase/Stripe credentials in tests
#   - CI uses these same mock values (see .github/workflows/ci.yml)
#   - Tests run against a local Next.js server (http://localhost:3000)
#
# See docs/ENVIRONMENT_VARIABLES.md for detailed configuration guide.
# ============================================================================

# ----------------------------------------------------------------------------
# FIREBASE CLIENT SDK (NEXT_PUBLIC_* exposed to browser)
# ----------------------------------------------------------------------------
# Mock Firebase project for testing - not a real Firebase instance
NEXT_PUBLIC_FIREBASE_API_KEY=ci-mock-api-key-for-testing
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=ci-mock-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=ci-mock-project
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=ci-mock-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:ci-mock-app-id

# Application URL for OAuth redirects
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------------------------------------------------------------------
# FIREBASE ADMIN SDK (Server-side only)
# ----------------------------------------------------------------------------
# Mock service account credentials for testing
# These allow CI builds to succeed without real Firebase access
FIREBASE_PROJECT_ID=ci-mock-project
FIREBASE_CLIENT_EMAIL=ci-mock@ci-mock-project.iam.gserviceaccount.com

# Mock private key (CI fallback mode will detect this and skip real auth)
# DO NOT replace with real key - tests should use mocks
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQC7VJTUt9Us8cKj\nMzEfYyjiWA4R4/M2bS1+fWIcPm15j9tXBWjdULHsUnDpJVBz0hLRqz4sLiJv7Jqx\n-----END PRIVATE KEY-----\n"

# ----------------------------------------------------------------------------
# STRIPE (Server-side only)
# ----------------------------------------------------------------------------
# Mock Stripe credentials for build/test environments
# Actual payments are NOT processed in tests
STRIPE_SECRET_KEY=sk_test_ci_mock_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_ci_mock_webhook_secret_for_testing
STRIPE_PRICE_ID_PRO=price_ci_mock_pro_subscription_id

# ----------------------------------------------------------------------------
# TESTING WITH REAL FIREBASE (OPTIONAL)
# ----------------------------------------------------------------------------
# If you want to test against a real Firebase project:
#   1. Create a separate Firebase project for testing (NOT production!)
#   2. Generate a service account key (IAM & Admin → Service Accounts)
#   3. Download JSON and extract values
#   4. Uncomment and fill in values below
#   5. Run: cp .env.test.example .env.test.local
#
# IMPORTANT: Never commit .env.test.local to git!

# NEXT_PUBLIC_FIREBASE_API_KEY=your-test-project-api-key
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-test-project.firebaseapp.com
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-test-project-id
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-test-project.appspot.com
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
# NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
# FIREBASE_PROJECT_ID=your-test-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-test-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_REAL_KEY_HERE\n-----END PRIVATE KEY-----\n"

# ----------------------------------------------------------------------------
# TESTING WITH FIREBASE EMULATORS (RECOMMENDED)
# ----------------------------------------------------------------------------
# For fully local testing without cloud services:
#   1. Install Firebase CLI: npm install -g firebase-tools
#   2. Initialize emulators: firebase init emulators
#   3. Start emulators: firebase emulators:start
#   4. Use emulator connection variables:
#
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
# FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199
#
# See: https://firebase.google.com/docs/emulator-suite
