# =============================================================================
# ShopMatch Pro - Environment Variables Template
# =============================================================================
#
# This file contains all required environment variables for the ShopMatch Pro
# job board application. Copy this file to .env.local and fill in the actual
# values from your Firebase and Stripe dashboards.
#
# CRITICAL SECURITY NOTES:
# - Never commit .env.local to version control
# - NEXT_PUBLIC_ variables are exposed to the browser (use only for public config)
# - Server-side variables (no NEXT_PUBLIC_) must remain secret
# - Validate setup with: npm run validate-env
#
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Configuration (Client-side)
# -----------------------------------------------------------------------------
# These variables are exposed to the browser and are safe for client-side use.
# Get these values from: Firebase Console → Project Settings → General

# Firebase API Key (Public - used for Firebase SDK initialization)
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here

# Firebase Authentication Domain (Public - domain for OAuth redirects)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com

# Firebase Project ID (Public - unique project identifier)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here

# Firebase Storage Bucket (Public - Cloud Storage bucket URL)
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com

# Firebase Cloud Messaging Sender ID (Public - for push notifications)
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here

# Firebase App ID (Public - unique application identifier)
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id_here

# -----------------------------------------------------------------------------
# Firebase Admin Configuration (Server-side ONLY)
# -----------------------------------------------------------------------------
# This project uses Firebase service account credentials for admin operations.
# Get these values from: Firebase Console → Project Settings → Service Accounts
# → Generate New Private Key (downloads a JSON file)
#
# CRITICAL: The private key must use escaped newlines (\n not actual newlines)
# Example format: "-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANB...\n-----END PRIVATE KEY-----\n"

# Firebase Project ID (Admin - must match your Firebase project)
FIREBASE_PROJECT_ID=your_project_id_here

# Firebase Service Account Email (Admin - from service account JSON)
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com

# Firebase Private Key (Admin - from service account JSON, must escape newlines)
# Copy the entire "private_key" value including "-----BEGIN" and "-----END"
# Keep all \n characters - do NOT replace with actual newlines
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# -----------------------------------------------------------------------------
# Stripe Configuration (Server-side ONLY)
# -----------------------------------------------------------------------------
# These variables handle payment processing and subscription management.
# Get these values from: Stripe Dashboard → Developers → API Keys

# Stripe Secret Key (Server-side - for payment processing)
# Use test key for development: sk_test_...
# Use live key for production: sk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe Webhook Secret (Server-side - for webhook signature verification)
# For LOCAL TESTING with Stripe CLI:
#   1. Install Stripe CLI: https://stripe.com/docs/stripe-cli
#   2. Run: stripe listen --forward-to localhost:3000/api/stripe/webhook
#   3. Copy the webhook signing secret (whsec_...) from the CLI output
# For PRODUCTION: Create webhook endpoint in Stripe Dashboard
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Pro Subscription Price ID (Server-side - from Products → Pricing)
# Create a subscription product in Stripe Dashboard, then copy the price ID
STRIPE_PRICE_ID_PRO=price_your_stripe_price_id_here

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Application Base URL (Public - used for redirects and links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Optional: Analytics and Monitoring (Add Later)
# -----------------------------------------------------------------------------
# Uncomment and configure these when ready for production monitoring

# Google Analytics ID (Public - for website analytics)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry DSN (Public - for error tracking and monitoring)
# Get this from: Sentry Dashboard → Settings → Projects → [Your Project] → Client Keys (DSN)
# Used by both client-side and server-side Sentry integrations
NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# -----------------------------------------------------------------------------
# Setup Instructions
# -----------------------------------------------------------------------------
#
# 1. Copy this file to .env.local:
#    cp .env.local.template .env.local
#
# 2. Fill in all required values from your Firebase and Stripe dashboards
#
# 3. Validate your configuration:
#    npm run validate-env
#
# 4. Test the health endpoint:
#    curl http://localhost:3000/api/health
#
# 5. Start development:
#    npm run dev
#
# For production deployment, ensure all variables are set in your hosting platform
# (Vercel, Netlify, etc.) with the exact same names and values.
# -----------------------------------------------------------------------------